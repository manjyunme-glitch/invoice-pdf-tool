# 更新记录

## v6.1.0 — 2026-07-27

这是一次聚焦界面一致性、可读性和步骤导航准确性的体验修复版本。

### 配色与可读性

- 阻止 `ttkbootstrap` 自动覆盖应用为原生 Tk 控件指定的颜色，恢复深色任务侧栏和完整主题层级。
- 为白天与黑夜主题统一调整主操作、成功、警告、危险和次级按钮颜色。
- 为禁用按钮显式设置高对比度文字，解决灰色按钮文字难以辨认的问题。
- 普通、悬停和禁用状态的按钮文字对比度均不低于 4.5:1。

### 排版与操作体验

- 保留“开始筛选并导出”等主操作按钮原有的 12 号粗体层级，不再被通用样式缩小。
- 提高页面副标题、流程说明、步骤描述和底部状态说明字号。
- 取消操作在可用时使用明确的危险色，打开目录与暂停等辅助操作降低视觉权重。

### 步骤导航

- 修正滚动画布定位分母错误，避免“输入”“规则”“结果”越往下偏移越大。
- “输入”和“规则”会准确滚动到对应标题；“预览”和“执行”定位结果区并聚焦固定操作栏中的相应按钮。
- 所有目标标题统一停在视口顶部 8px，窗口缩放后仍保持一致。

### 质量保障

- 新增步骤定位、主题配色不被覆盖、按钮对比度、禁用态可读性和主按钮字号回归测试。
- 完整测试套件扩展至 145 项。

## v6.0.0 — 2026-07-22

这是一次以文件安全、连续工作流和可恢复性为核心的版本升级。

### 文件安全与恢复

- 移动、复制、报告和撤销均使用受信根目录与 SHA-256 指纹校验。
- 内容不同的同名文件不会被覆盖；重复发票号不会随机选择 PDF。
- 任务期间持续写入恢复日志，异常退出后可恢复已完成操作。
- 恢复日志损坏时隔离原文件；较新格式日志保持只读并阻止旧程序覆盖。
- 恢复日志写入失败时立即还原当前移动或复制，避免形成未登记的文件变更。
- 配置、历史和任务日志使用原子保存。

### 工作流与界面

- 新增任务式左侧导航和连续的输入、规则、预览、执行、结果流程。
- PDF 扫描、Excel 分析和匹配预览在后台运行。
- 正式写入任务支持暂停、继续、取消、逐文件失败说明和安全重试。
- 筛选执行必须基于当前输入完成预览，并通过最终确认。
- 历史页支持搜索、详情、可信回滚和“仅载入、不自动执行”的再次执行。
- 支持浅色/深色主题、键盘导航和 Windows DPI 缩放。

### 配置、日志与诊断

- 配置支持版本校验、导入、导出、备份、恢复默认和未来版本写保护。
- 日志统一脱敏并按大小轮换；诊断包不包含原始业务文件或配置历史。
- 首选配置目录不可用时可以降级启动，并明确显示权限诊断信息。

### Excel、性能与兼容性

- 完善 `.xlsx` / `.xls` 依赖检测和打包。
- 加强空表、缺失列、数字标题、前导零、科学计数法和空值处理。
- 大型 Excel 行处理改用低开销元组迭代。
- 递归 PDF 扫描会提前剪枝导出目录和链接目录，并支持扫描中取消。
- `tkinterdnd2` 初始化失败时自动降级到原生 Tk 窗口。

### 兼容性说明

- 旧版 `%APPDATA%\InvoiceTool\config.json` 和 `history.json` 原位读取，不做破坏性迁移。
- 旧历史记录仍可查看；缺少安全指纹的旧记录不会执行自动回滚。
- `发票处理工具v5.py` 保留为兼容入口，新版本入口为 `发票处理工具v6.py`。
